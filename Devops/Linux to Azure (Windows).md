#azure 
NLB, ALB -> Azure LB (layer 4, 7)
ECS -> Azure Container Instance
ECR -> Azure Container Registries
File Descriptor -> Handle (window) to handle files and sockets
ulimit -> regedit
Reserve Proxy -> ACS (Nginx)
Azure Webpubsub: Websocket gateway + pub/sub + big scale + fanout
Azure Container Apps – Session Pools: for session application like online IDE code, AI session, data processing & response
DynamoDB -> Azure Cosmos DB
Lambda -> Azure Functions
Api Gateway -> Azure API Management
EventBridge -> Azure Event Grid
StepFunctions -> Azure Durable Functions
IAM -> Entra ID (IAM Users), Azure RBAC (IAM Roles), IAM Role for Service (Managed Identity)
Role chaining: Azure does not support role chaining.
SQS: Service Bus

Azure Automation Account >> Patching updates with PS scripts
Image Baking (Packer) -> Run PS scripts to create Golden Image